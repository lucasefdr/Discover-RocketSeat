// for...of
let name = "Lucas";
let names = ["Lara", "Renata", "Eduardo"];

// retorna todas as letras de name
for (let char of name) {
  console.log(char);
}

//retorna todos os names da lista names
for (let name of names) {
  console.log(name);
}
