/*
    Object

    Objeto com propriedades/atributos (tamanho, cor) e funcionalidades/métodos

    { propriedade: "valor"}

*/

console.log({
  name: "Lucas",
  idade: 24,
  andar: function () {
    console.log(`${name} está andando...`);
  },
});
